.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
dist/
/build/
*.egg-info/
# Blender/agent run artifacts
runs/
*.blend1
# regenerable asset cache (model.glb is the pinned input; raw.glb is not)
shots/*/assets/*/raw.glb
nohup.out
# shot folders: local working data (briefs/refs/outputs) — not tracked
shots/
# stray probe renders at the repo root (shots keep their own)
renders/
# ledger write-lock sidecars and in-flight atomic writes
*.json.lock
*.json.tmp.*
# Generated renders, transcripts, probes, and evaluation runs are local evidence.
# Tracked evaluation definitions and small fixtures live under evals/.
artifacts/

# Editor logs written into the checkout root.
nvim.log
